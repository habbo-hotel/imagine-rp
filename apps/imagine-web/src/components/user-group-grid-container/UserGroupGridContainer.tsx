import React from 'react';
import { UserGroupGridContainerProps } from './UserGroupGridContainer.types';
import { UserGroupGridContainerElement } from './UserGroupGridContainer.styled';

export function UserGroupGridContainer({ group }: UserGroupGridContainerProps) {
  return (
    <UserGroupGridContainerElement>

    </UserGroupGridContainerElement>
  )
}