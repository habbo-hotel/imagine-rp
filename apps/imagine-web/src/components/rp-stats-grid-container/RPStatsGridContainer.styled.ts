import styled from "styled-components";
import { ProgressElement } from "../progress/Progress.styled";

export const HealthProgress = styled(ProgressElement)`
`

export const EnergyProgress = styled(ProgressElement)`
`

export const HungerProgress = styled(ProgressElement)`
`

export const ArmorProgress = styled(ProgressElement)`
`